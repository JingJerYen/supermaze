# Maps

Hand-made, pre-validated maps. One JSON file per map.

The data format is defined by `MapData` in `packages/sim/src/map/types.ts` and
checked by `tools/map-validator`. Every map must pass validation before it can be
added to a map pool.

No maps exist yet; the format will be finalised during phase 1.
