# Super Maze / Maze Tower

> 狀態：需求初稿已完成，技術棧已於第 17 節定案，階段 0 與 1 已完成，目前位於第 18 節的階段 2。專案名稱與部分規則尚未定案。
>
> 本文件是提供 Codex 與其他 AI 開發工具使用的專案規格。所有 Agent 在修改程式前都必須先閱讀本文件。若需求標示為「待確認」，不得自行假定答案並將其做成不可逆的架構。

## 1. 專案概念

- 暫定名稱：`Super Maze` 或 `Maze Tower`。
- 遊戲類型：競速型走迷宮遊戲。
- 平台：網頁遊戲。
- 視覺形式：2.5D，但畫面觀感接近 3D。
- 支援單人遊玩與多人連線。
- 美術重點放在人物與道具；迷宮環境以簡單、清楚、低成本的幾何色塊呈現。

## 2. 玩家與隊伍

- 單人模式：1 名真人對 1 名 CPU，固定 1v1；此時對局有效參賽者為 2 人。
- 多人模式：2～6 名真人玩家，不加入 CPU 補位。房間人數不足時等待真人加入，不以 CPU 補足。
- CPU 只在兩種情況出現：單人模式的對手，以及對局中接管斷線玩家。
- 對局中真人玩家斷線時，CPU／AI 必須立即接管該玩家原本的角色。
- CPU 接管後必須保留角色位置、隊伍、鑰匙、道具、分數、效果與其他對局狀態，不能建立新的替代角色。
- 真人在該局結束前重新連線時，可以取回同一角色的控制權。
- 斷線不會降低該隊的有效人數或全員登塔門檻，避免玩家利用斷線改變勝利條件。
- 每場固定只有兩支隊伍，不支援三隊以上或人人各自成隊。
- 兩隊人數差最多 1 人。多人模式的合法隊形因此為 1v1、2v1、2v2、3v2、3v3。
- 允許隊差一人的理由：人多的隊必須全員登塔才算完成，人數優勢與集合難度互相抵銷，不視為先天不平衡。
- 正式配對與私人房套用相同的隊形規則，私人房沒有額外的自訂隊伍配置。
- 同一隊的玩家互相合作；兩隊彼此競爭。

### 2.1 大廳與開局（2026-09-26 定案並實作）

- 進入遊戲先輸入暱稱，再選「快速配對」或「私人房」。私人房產生四碼房間代碼供朋友加入。
- 房間內固定兩隊並排顯示。新加入者自動進入人數較少的隊；玩家可換隊，會使隊差超過 1 的換隊被拒絕。
- 最低開局人數為 2（1v1 即可開局）。
- 每位玩家有「準備」狀態。人數達到最低門檻且全員準備後開始 10 秒倒數；倒數期間有人取消準備或離開即中止。倒數歸零由伺服器開局並鎖定房間，之後不再放人進來。
- 快速配對房間人數滿 6 時直接倒數，不等準備。
- 快速配對等待 60 秒仍不足 2 人時，提示玩家退出；現階段不以 CPU 補位（CPU 對手排在第 18 節的階段 5）。
- 私人房房主為第一位進房者，可在滿足最低人數與隊差規則時手動開始；房主離開時由下一位進房者接任。
- 回合結束顯示結算約 15 秒，之後回到同一房間的大廳，可再開一局或離開。
- 地圖與旋轉方向在開局那一刻依對局種子抽選。模擬層不參與大廳邏輯。
- 實作：一個 Colyseus 房間跨多場對局，狀態機為 lobby → countdown → playing → results → lobby；大廳為房間自有狀態，比賽開始時才建立 Simulation，結束後丟棄。快速配對用 `mode` 過濾只配到快速房，私人房以 metadata 的四碼代碼配對。比賽中鎖房，回到大廳解鎖。中途主動離開者轉為 CPU 且不開重連視窗；斷線者轉 CPU 並保留重連視窗。倒數、結算與快速配對等待時間為伺服器端常數（10 秒、15 秒、60 秒）。純規則在 `apps/server/src/lobbyLogic.ts`，有單元測試。

## 3. 核心勝負規則

- 所有隊伍或個人的主要目標，是探索迷宮、取得鑰匙、返回中心塔並登上塔頂。
- 每位玩家取得可用的鑰匙後，才能回到中心塔解鎖自己的登塔資格並登上塔頂。
- 多人隊伍必須讓全體隊員都抵達塔頂集合，才算完成該層迷宮。
- 第一支全員在塔頂集合的隊伍成為該層的獲勝隊伍。
- 出現獲勝隊伍後，其他隊伍與玩家仍可繼續登塔並依抵達名次得分，不會因第一隊獲勝而立即失去全部得分機會。
- 單人一隊時，玩家取得鑰匙並抵達塔頂，即完成該層迷宮。
- 每局設有時間限制。初步構想約為 10～15 分鐘，但不得將此數值寫死。
- 回合時間應能依參賽者數量、迷宮尺寸與難度調整，並允許開發者直接覆寫以便試玩和平衡。
- 若時間耗盡時仍沒有任何隊伍全員登頂，先以「已登上塔頂的隊員人數」最多者為獲勝隊伍。兩隊人數不同（例如 3v2）時，是比較登塔人數還是登塔比例，待確認；目前以調校參數 `round.timeoutClimbMetric` 切換，預設 `count`。
- 若多隊的登塔人數相同，再比較各隊尚未套用勝隊倍率前的累積總分；分數較高者獲勝。
- 若登塔人數與倍率前總分都完全相同，則比較各隊達到目前登塔人數的時間；較早達成者獲勝。
- 超時判定不進入延長賽，也不產生並列勝隊。
- 回合在所有有效玩家都完成登塔或時間耗盡時結束；是否還需要其他提前結束條件待確認。

### 3.1 計分規則

- 所有隊伍與玩家都可以取得分數，不再採用只有獲勝隊伍得分的規則。
- 每位玩家成功登上塔頂時，依實際登頂順序取得名次分數；越早抵達，分數越高。
- 找到鑰匙會增加個人分數。
- 玩家登塔時，背包內尚未使用的道具會轉換成額外結算分數。
- 鬼抓人事件中，鬼玩家成功抓到逃亡玩家時會取得額外分數。
- 第一支全員登上塔頂的隊伍為獲勝隊伍；該隊成員在本局取得的分數套用 `2×` 勝隊加成。
- 落敗隊伍仍保留其登頂、尋找鑰匙及鬼抓人等行為所取得的分數。
- 各名次分數、鑰匙分數、抓人分數、勝隊倍率及分數套用順序都必須資料化，方便試玩調整。
- 登塔時不分道具種類，每件剩餘道具都提供相同的固定結算分數；一個量子傳送端點視為一件道具。
- 每件道具的固定結算分數必須資料化，不能依程式中的道具類型硬編碼不同價值。
- 每位玩家只能因首次取得自己的鑰匙獲得一次鑰匙分數，不能藉由重複拾取刷分；CPU 是否列入正式排名尚待確認。

## 4. 每層的核心遊戲循環

1. 所有玩家由該層的中心塔出發。
2. 玩家進入中心塔周圍的迷宮探索。
3. 玩家尋找能夠解鎖中心塔的關鍵鑰匙。
4. 取得鑰匙的玩家可以選擇：
   - 先留在迷宮中協助尚未取得鑰匙的隊友；或
   - 返回中心塔，使用自己的鑰匙解鎖並登上塔頂。
5. 已登上塔頂的玩家切換成俯瞰視角，可以觀察整座迷宮的地形，並以語音、文字或其他尚待定義的溝通方式指引隊友。
6. 當第一支隊伍的全部成員都抵達塔頂後，該隊完成集合並成為獲勝隊伍，取得勝隊分數加成。
7. 其他隊伍可在剩餘時間內繼續尋找鑰匙並登塔，爭取後續名次和個人分數。
8. 本局結算後，玩家前往下一座塔或中心塔的下一層，開始下一輪迷宮；關卡銜接方式待確認。

## 5. 鑰匙與中心塔

- 鑰匙是完成迷宮的必要關鍵物件，不只是額外收集品。
- 玩家必須先離開中心塔探索迷宮，取得鑰匙後再返回中心塔。
- 每位玩家都必須親自在迷宮中找到一把鑰匙，才能取得自己的登塔資格。
- 玩家取得鑰匙後，該鑰匙綁定本人，不能轉交給其他玩家，也不能放入隊伍共享池。
- 每位玩家同時最多持有一把鑰匙；已持有鑰匙的玩家不能額外拾取或囤積其他鑰匙。
- 地圖上的未拾取鑰匙沒有預設的隊伍或玩家歸屬；任何尚未持有鑰匙的玩家都可以競爭拾取。
- 鑰匙在被拾取的瞬間才綁定拾取者本人。
- 中心塔具有與個別玩家對應的解鎖機制；例如 4 人隊需要四名隊員各自帶回一把鑰匙並分別完成登塔。
- 每局生成的鑰匙總數必須剛好等於該局有效參賽者人數，不生成額外備用鑰匙。
- 由於每位玩家最多只能持有一把鑰匙，其他玩家不能囤積而造成某位玩家永久缺少鑰匙；但所有生成的鑰匙都必須被找到，才能讓全體玩家完成登塔。
- 玩家使用自己的鑰匙後，可以先行登上塔頂，不必等待其他隊員同時返回。
- 暫定實作（正式操作方式仍列於第 16 節待確認）：持鑰匙的玩家站在與塔占地相鄰的道路格上，主動送出「登塔」動作即登塔；不採用走進塔內自動登塔，以避免誤觸這個不可逆決定。桌機為 E 或空白鍵，手機為畫面上只在可登塔時出現的按鈕。
- 已取得鑰匙的玩家可以暫時不登塔，繼續在迷宮中協助隊友。
- 鑰匙不能轉交或由隊伍共享。
- 玩家使用鑰匙登上塔頂後不能返回本層迷宮；登塔是不可逆的策略決定。
- 玩家若想繼續親自在迷宮內協助隊友，必須先保留鑰匙並延後登塔。
- 已登塔玩家永久轉為本層的俯瞰指揮角色，不能再參與鬼抓人、拾取物件或重複取得登塔分數。
- 玩家登塔時，背包內所有尚未使用的道具都會立即消失，並依規則轉換成個人結算分數；道具不會掉在塔入口或保留於塔頂。
- 若消失的背包物品包含已配對的量子傳送端點，地圖上的另一端會立即失去連線並回到等待配對狀態。

## 6. 地圖與垂直結構

- 迷宮幾何採人工製作的固定地圖，不使用每局從零生成的程序化迷宮。
- 遊戲應維護多張已驗證的地圖池，每局開始時從符合模式、玩家數與難度的候選地圖中抽選一張。初步目標為 12 張手繪地圖。
- 每張地圖可以四個方向擺放：每局依對局種子從 0、90、180、270 度中抽選一個旋轉角度，對玩家而言 12 張地圖有 48 種外觀。旋轉由 `rotateMap` 純函式處理，樓梯方向與橋的走向從鄰格自動推得，地圖資料只需旋轉格子與生成點座標。
- 地圖最外圈一定是牆，不使用空白格當邊界；外圈牆頂是否可經由樓梯抵達由各地圖自行決定。
- 每張地圖必須預先驗證道路層、牆頂層、樓梯、橋梁、中心塔、碰撞、導航與必要路徑的正確性。
- 固定地圖必須定義足夠的合法生成點，供鑰匙、道具箱、電燈開關及其他動態物件使用。候選點以標記字元直接畫在版面上（大寫在道路、小寫在牆頂：`K`/`k` 鑰匙、`B`/`b` 道具箱、`L`/`l` 開關），載入時換算成座標；也可用座標列表。三類候選不得共用格子，也不得落在塔入口格；每局依種子從候選中抽出所需數量，道具箱補箱同樣只從仍空著的候選抽。
- 鑰匙位置不固定：每張地圖由作者預先標記一組經過驗證的合法鑰匙生成點。
- 每局開始時，系統依可重現的對局種子，從合法生成點中隨機抽選與有效參賽者人數相同的位置，且同一位置不能重複生成多把鑰匙。
- 鑰匙生成點必須可抵達、彼此分散，並涵蓋道路層與牆頂層；實際分布比例由地圖資料定義。
- 每座迷宮的中心設有一座中心塔。
- 每局開始時，玩家會從中心塔出來，接著進入迷宮競賽。
- 地圖是均勻的單位方格：每格一個單位立方體，牆是一格厚、一格高的方塊，牆頂就是它的上表面；道路、樓梯、橋各占一格。不使用非整格的幾何。
- 走道寬度一律一格，只容一個角色通行；牆一律一格厚。地圖上不得出現 2×2 全為道路層可走格或 2×2 全為牆頂層可走格的區塊。唯一例外是中心塔周圍的廣場，由地圖資料以 `plazaRadius` 標記範圍。
- 一格寬走道是道具設計的前提：障礙物「完整封住通道」與單向門「只能單向通過」只在一格寬時成立。
- 手繪地圖建議採奇偶格子法：地圖邊長取奇數，奇數座標為路口、偶數座標為牆或牆的缺口，可保證走道一格寬、牆一格厚。此為繪製慣例，驗證器只檢查寬度規則本身。
- 角色之間目前互相穿透，同一格可同時站多名角色。角色碰撞是否加入，留到道具系統完成後與道具碰撞判定一併決定（見第 16 節）。
- 迷宮採簡化的兩層高度設計，不追求深層或複雜的垂直建築：
  1. 地面道路層：玩家可在迷宮道路上移動。
  2. 圍牆頂層：道路旁的牆體高一層，玩家也可以在牆頂行走。
- 玩家不能直接從道路任意走上圍牆。
- 玩家必須找到樓梯，才能從道路層登上圍牆頂層。
- 地圖可以包含架設在不同圍牆之間的橋。橋讓玩家從一段牆頂跨越到另一段牆頂，不必先返回道路層。
- 樓梯是道路層與牆頂層之間的合法轉換點；橋則是牆頂區域之間的連接路徑。
- 角色沒有自由跳躍能力，也不能從牆頂直接跳落到道路層。
- 移動採「點一下轉身、按住才走」（2026-09-26 定案）：站定時推向與面向不同的方向，角色立刻轉身但不移動，方向持續按住超過 `movement.turnDelaySec`（初始 0.12 秒）才開始走；提前放開就只轉身。已在走動中於格子邊界改變方向不套用延遲。目的是讓玩家能原地轉頭決定道具放置方向。
- 道路層與牆頂層之間只能透過樓梯雙向移動；不同牆頂區域只能經由合法橋梁或其他明確連接路徑通行。
- 場景邊緣和碰撞必須防止角色意外跌落、翻越牆面或利用物理效果繞過迷宮拓撲。
- 中心塔同時是起點、鑰匙解鎖處、登塔入口與隊伍終點集合區。
- 塔頂必須容納完整隊伍，並能明確顯示哪些隊員已經抵達。
- 塔頂是可行走的平台，屬於第三個層 `towerTop`：範圍為塔占地向外擴一圈的格子（3×3 占地即 5×5 平台），四周不能走出去。登塔者依名次由中心向外依序就位，之後可在平台上自由移動，但不能再回到迷宮，也不能拾取、放置或觸發任何物件。
- 塔頂提供可觀察整座迷宮的俯瞰視角；一般在迷宮中的玩家維持受限的 2.5D 視角，不能直接看見整張迷宮。
- 每一層或每一輪都會以中心塔為核心。完成後前往「下一個塔」或「塔的下一層」的確切空間關係尚待確認。

## 7. 攝影機與資訊差

- 一般探索狀態使用 2.5D、接近 3D 的遊戲視角。
- 一般玩家的視野受到攝影機及迷宮牆面限制，無法一次掌握整張地圖。
- 已抵達塔頂的玩家會取得俯瞰視角，可以理解迷宮的整體配置。
- 俯瞰資訊是團隊合作機制的一部分：先登頂者可充當觀察員，協助仍在迷宮中的隊友尋找路線或鑰匙。
- 塔頂俯瞰視角提供完整地圖情報，包括：
  - 全部迷宮地形、道路、牆頂、樓梯與橋梁。
  - 自己的隊友及所有敵隊玩家位置。
  - 所有尚未拾取的鑰匙與場上道具。
  - 電燈開關、陷阱、單向門、障礙物與傳送端點等互動物件。
  - 隊伍記號及其他之後加入的關鍵動態資訊。
- 塔頂俯瞰是玩家提早登塔的主要獎勵，目的在讓該玩家成為完整情報指揮者，而非單純觀戰者。
- 迷宮內玩家的小地圖極簡（2026-09-26 定案）：只有地圖外框、自己的位置點與中心塔圖示。不顯示迷宮版面、已探索區域、隊友、敵隊、鑰匙、道具箱或開關，任何形式都不准。
- 已登塔的玩家小地圖顯示完整版面、所有玩家與未使用的開關，與其俯瞰視角一致。
- 不提供任何雷達式方向箭頭。
- 不得在牆面或牆頂上繪製座標或任何文字（2026-09-26 決定）。棋盤座標函式保留供日後記號系統使用；塔頂玩家的名單中仍顯示各玩家座標。
- 本機玩家登塔後，鏡頭在約一秒內平滑切換為地圖中心正上方的垂直俯視，高度依地圖尺寸與畫面比例自動計算使整張地圖入鏡，北方朝上；鏡頭不再跟隨角色。塔頂玩家在平台上的移動仍會顯示給所有人。

## 8. 全圖燈光機制

- 迷宮中分布可供玩家操作的電燈開關。
- 每局開始時，地圖預設為正常照明狀態。
- 遊戲至少有兩種全圖燈光狀態：
  - `LIGHT_ON`：地圖正常照明，玩家依一般 2.5D 視角觀看環境。
  - `LIGHT_OFF`：整張地圖被黑暗遮蔽，每位玩家只能看見自己周圍一小塊圓形區域。
- 黑暗中的可見區域以玩家角色為圓心，半徑必須做成可調參數。
- 玩家操作關燈開關後，整個地圖對所有玩家進入黑暗狀態。
- 黑暗會持續存在，直到有玩家找到另一個可用開關並將燈重新打開。
- 燈光狀態是全體共享的地圖狀態，不限於觸發者或特定隊伍。
- 每個電燈開關只能使用一次；玩家操作時會反轉目前的全圖燈光狀態，之後該開關永久失效。
- 開關只存在於道路層。它的邏輯位置是玩家站立的道路格，面板畫在相鄰牆面的立面上；掛哪一面牆在載入（旋轉之後）依可見度自動推導，北牆優先、其次東西牆、最後南牆。候選格四鄰都沒有牆時驗證器拒絕。
- 為了在四個旋轉方向與黑暗中都找得到，未使用的開關在道路格上有地面光暈，用過後光暈與面板一併熄滅。
- 操作開關需要玩家站定在該格並按下情境動作鍵（桌機 E 或空白鍵、手機的畫面按鈕），不會因走過而觸發。登塔與開關共用同一顆動作鍵，由權威模擬依所在格判定要做哪一件事。
- 同一個開關不能反覆開關燈。若要再次切換燈光，玩家必須找到另一個尚未使用的開關。
- 地圖生成必須讓尚未使用的必要開關保持可抵達，不能因地形或永久狀態造成無法切換。
- 每局生成的電燈開關總數必須是偶數，且至少包含兩個開關。數量由各地圖的 `lightSwitchCount` 指定，開關位置每局依種子從該地圖的候選點抽選；地圖作者藉此依尺寸與難度決定開關數。
- 因地圖從亮燈開始並在每次使用時交替明暗，最後一個開關必然只會在黑暗狀態下使用，作用是將燈重新打開。
- 所有開關用完後，地圖必須維持亮燈至本局結束，不允許永久黑暗。
- 關燈對塔頂玩家同樣生效：完整俯瞰情報暫時被黑暗遮蔽，只能看見以塔頂玩家為中心的局部圓形區域。
- 塔頂黑暗可見半徑必須獨立設為可調參數，不必與迷宮內玩家的半徑相同。
- 地圖重新開燈後，塔頂玩家立即恢復完整俯瞰情報。

## 9. 背包與通用道具規則

- 每位玩家最多同時攜帶 3 個道具。
- 背包容量必須做成可調參數，不得在多處硬編碼。
- 背包是先進先出的隊列：道具只能依取得順序使用，永遠使用最早取得的那一件，玩家不能挑選格子，也不能丟棄道具。
- 背包已滿時，玩家不能取得或開啟道具箱，也不能預覽箱內的隨機結果；道具箱保持原狀留在地圖上。
- 玩家必須先使用一件道具，空出背包格後才能取得道具箱。
- 使用道具與登塔、開關共用同一顆情境動作鍵（桌機 E 或空白鍵，手機為單一畫面按鈕）。優先順序：站在塔入口且持鑰匙時登塔；站在未使用的開關格時切燈；否則使用背包最早的道具。介面必須顯示這一按會做哪件事。需要放置位置的道具，按下後進入放置流程，細節在各道具規則中定義。
- 將量子傳送端點部署成地板方塊屬於設備放置。
- 除量子傳送端點外，其他道具原則上都是一次性消耗品，成功使用後立即從背包消失。
- 除量子傳送端點外，放置型道具使用後會在場景中存在一段時間，再自動消失。
- 放置型道具的初步存在時間可以從約 10 秒開始試玩，必須依道具種類分別設定，實際數值待測試決定。
- 任何放置型道具都不能造成永久封路；時間結束後必須可靠地自動移除。
- 放置位置也必須受規則檢查，不能永久封鎖中心塔、唯一樓梯、唯一橋梁、鑰匙或其他必要目標。
- 放置方式固定：放置型道具與傳送端點一律放在角色面前一格、同一層，方向取角色當下面向，按下情境動作鍵即放，不提供自由選位或預覽。角色面向由最後一次推動方向決定。
- 放置目標格的限制：必須可通行；不能是樓梯、塔入口格；不能是任何鑰匙、道具箱或電燈開關的候選格；不能有道具箱、未拾取鑰匙、其他放置物或傳送端點；不能有玩家站著或正走進來。不符合時不提供動作：情境按鍵不顯示、按鍵無效、道具留在背包，不做放置預覽也不顯示原因文字（2026-09-26 定案）。鐵鎚不受此限，隨時可揮。
- 玩家主要透過分布在迷宮中的道具箱取得道具。
- 場上同時存在的道具箱數量固定為「本局有效參賽者人數 × 2」。例如 8 名參賽者的對局維持 16 個道具箱。
- 道具箱內容採加權隨機抽取；玩家開啟前不知道會取得哪一種道具。
- 各道具的抽取權重必須集中設定，方便依模式、玩家數、地圖大小和試玩結果調整。
- 量子傳送端點具有隊伍數量限制：每支隊伍同時最多存在兩台，也就是一組完整傳送設備。
- 計算上限時，隊員背包中與地圖上已放置的端點都必須計入。
- 當隊伍已有兩台傳送端點時，道具箱不能再為該隊玩家抽出新的傳送端點。
- 若端點因玩家被鬼抓到或攜帶端點登塔而永久消失，該隊現存端點數量隨之下降；傳送端點重新加入該隊的隨機道具池，使隊伍有機會補回兩台。
- 道具箱被玩家取得後，原箱子消失，系統必須在另一個合法隨機位置生成替代箱，使場上道具箱總數恢復到目標值。
- 隨機位置必須位於可行走且可抵達區域，不能與玩家、中心塔、鑰匙、其他箱子或場景物件重疊。
- 替代箱必須在舊箱被取得後立即生成，不設刷新延遲；場上道具箱數量應始終維持目標值。
- 取得舊箱、抽取道具與生成替代箱必須由同一套權威遊戲狀態依序處理，避免多人同時拾取造成重複獎勵或箱子數量錯誤。

## 10. 初步道具清單

### 10.1 單向門

- 玩家可以在迷宮的合法位置安放一扇單向門。
- 單向門只允許角色朝指定方向通過，不能從反方向穿越。
- 單向通行規則對所有玩家生效，包括放置者及其隊友；不提供隊伍免疫。
- 允許通行方向由放置者當下的角色面向決定：從玩家背後朝玩家面向的前方可以通行，反方向被阻擋。
- 放置確認前必須顯示清楚的門位置預覽與方向箭頭，讓玩家知道允許通行的方向。
- 單向門存在一段可設定的時間後消失。

### 10.2 障礙物

- 玩家可以在迷宮的合法位置放置障礙物以阻擋通路。
- 障礙物會完整封住所在通道，任何玩家都不能穿越、側身繞過或直接推開。
- 障礙物存在一段可設定的時間後消失，或被有效的破壞手段提前移除。
- 障礙物阻擋所有玩家，包括放置者及其隊友；不提供隊伍穿透。
- 玩家只能等待障礙物生命週期結束，或使用鐵鎚將其提前破壞。

### 10.3 鐵鎚

- 鐵鎚是一次性道具，可用來敲碎角色面前一格上任何由玩家放置的物件：單向門、障礙物、定身陷阱，以及任何隊伍的量子傳送端點（2026-09-26 定案，取代原本只能敲障礙物的規則）。
- 使用即消耗：揮空、目標無效或目標已被其他玩家移除時，鐵鎚同樣消失。理由：背包是先進先出隊列，若鐵鎚揮空不消耗，排在最前面的鐵鎚會在沒有障礙物時卡住整個背包（2026-09-26 定案）。
- 鐵鎚不能破壞迷宮牆體、道具箱、鑰匙、電燈開關、樓梯、橋梁或其他場景物件。
- 傳送端點被敲毀時，地圖上的另一端立即失去連線並回到等待配對狀態；該隊現存端點數下降，傳送端點重新加入該隊的道具池。

### 10.4 定身陷阱

- 玩家可以放置定身陷阱。
- 其他角色走到陷阱上時，會被固定在原地一段時間。
- 陷阱在場上的存在時間與命中後的定身時間是兩個獨立參數。
- 陷阱在成功觸發第一名玩家後立即消失，不能在存在期間重複抓人。
- 若一直沒有玩家觸發，陷阱會在可設定的存在時間耗盡後自動消失。
- 定身陷阱會被任何玩家觸發，包括放置者及其隊友；不提供隊伍免疫。

### 10.5 量子通訊／傳送機

- 量子通訊機是成對使用的隊伍專屬可回收設備，是一般一次性道具規則的例外。
- 每一台傳送端點都是一件獨立道具，各自占用玩家背包的一格容量。
- 同一隊必須取得兩台獨立傳送機，才能建立一組完整傳送連線。
- 兩台機器可以由同一名玩家或兩名不同隊友分別攜帶與放置。
- 第一個端點放下後處於等待配對狀態；同隊放置第二個端點時，依放置順序自動與最早尚未配對的端點連接並啟用。
- 傳送端點可放在道路層、牆頂層與橋面等所有合法可行走地面，因此同一組端點可以建立跨層捷徑。
- 中心塔內部、塔頂、牆外、不可達區域、物件重疊位置及空間不足處禁止放置端點。
- 地板端點本身不能阻擋玩家通路；玩家可直接走過並在符合權限時觸發傳送。
- 玩家從道具箱取得傳送端點時，端點會先進入該玩家背包並占用一格。
- 玩家選擇放置後，端點會顯示為地板上的方塊磚，並使用所屬隊伍的顏色清楚標示。
- 地圖上的傳送方塊及其隊伍顏色對所有玩家可見，包括敵隊。
- 放在地上的端點不再占用原攜帶者的背包格。
- 同隊任何成員都可以將地上的己方端點撿回自己的背包，再搬運並放置到其他合法位置；端點是隊伍共有設備，不綁定最初拾取者。
- 敵隊不能撿取、移動或使用其他隊伍的傳送端點，但可以用鐵鎚敲毀它，也可根據可見端點推測對方的傳送路線。
- 若同隊成員撿起已啟用傳送連線中的任一端點，該組連線立即暫停；端點重新放置並恢復有效配對後，傳送功能才重新啟用。
- 傳送機採雙向傳送：同隊玩家進入任一端點後，都會瞬間抵達另一個端點。
- 其他隊伍不能使用該傳送裝置。
- 玩家抵達出口端點時，必須避免立刻觸發反向傳送；可要求玩家先離開端點觸發區，或使用極短的防循環保護。
- 端點成功建立連線後，在有效期間內可供所屬隊伍不限次數使用。
- 傳送端點取得後原則上會持續存在到本局結束，不因放置、配對或經過時間而自動消失；但玩家攜帶端點時被鬼抓到或登上塔頂，端點會隨背包清空而永久消失。
- 若被清除的端點原本屬於一組有效配對，留在地圖上的另一端不會消失，但會立即失去連線並回到等待配對狀態。
- 未配對端點可永久等待第二個端點；已配對端點可持續運作，直到被隊友撿起、改放位置或本局結束。
- 本局結束時，所有仍在背包或地圖上的傳送端點都隨本局狀態一併清除。
- 除防止立即反向循環外，目前不設正式使用次數限制或長時間冷卻。

## 11. 隊伍記號系統

- 做記號是所有玩家都具有的基本能力，不占用道具背包。
- 做記號沒有使用次數限制。
- 每位玩家同時最多保留 3 個有效記號；此上限按玩家個別計算，不是全隊共用。
- 玩家放置第 4 個記號時，系統自動移除該玩家目前最舊的記號。
- 記號沒有自動過期時間，會持續保留直到玩家手動取消、被該玩家的新記號取代，或本局結束。
- 記號只對放置者所屬隊伍可見，敵隊看不到。
- 記號固定提供五種類型：`前往`、`危險`、`鑰匙`、`道具`、`開關`。
- 記號不附帶自由文字，也不提供更大型的圖示輪盤。
- 迷宮中的玩家與塔頂俯瞰玩家都能使用相同的五種隊伍記號。
- 遊戲內建隊伍專屬的快捷訊息系統，供迷宮玩家與塔頂指揮者溝通。
- 快捷訊息使用預先定義、可本地化的短句，例如「往左」、「往右」、「前方危險」、「找到鑰匙」或「來這裡」。
- 正式版本目前不內建自由文字聊天或語音聊天；玩家若需要語音，可自行使用外部通訊工具。
- 敵隊不能看見己方記號或收到己方快捷訊息。

## 12. 可調式遊戲參數

- 所有會影響平衡的數值必須集中在明確的設定檔或調校模組中，便於不改動核心邏輯就能反覆試玩。
- 至少必須能調整：
  - 回合時間上限；開發者可在建立對局時直接覆寫。
  - 超時判定時登塔人數的比較方式（人數或比例）。
  - 依玩家數、隊伍數、迷宮尺寸與難度計算時間的規則。
  - 迷宮尺寸與難度。
  - 不同模式、玩家數與難度可抽選的固定地圖池。
  - 參賽者人數上下限，以及兩隊人數差上限；正式規則目前為最多 6 人、隊差最多 1。
  - 移動速度與轉身延遲。
  - 鑰匙數量。
  - 玩家背包容量。
  - 道具箱中各道具的抽取權重。
  - 每名有效參賽者對應的場上道具箱數量；正式規則目前為 `2`。
  - 黑暗狀態的個人可見半徑。
  - 塔頂玩家在黑暗狀態下的可見半徑。
  - 各類放置物的存在時間。
  - 定身陷阱的定身時間。
  - 鬼抓人事件的觸發間隔、預告時間、持續時間與抓人分數。
  - 被抓後的原地定身時間與防重抓保護時間。
  - 鬼相對於一般玩家的移動速度倍率；初始試玩範圍約為 `1.10×～1.15×`。
  - 各登頂名次的分數。
  - 找到鑰匙的分數。
  - 登塔時每件剩餘道具轉換的結算分數。
  - 勝隊分數倍率；初步規則為 `2×`。
- 設定必須有清楚的預設值、單位與合理範圍；暫定數字只能視為初始試玩值。
- 隨機或動態規則仍必須能以明確的種子或測試設定重現，方便除錯與平衡驗證。

## 13. 週期性鬼抓人事件

- 一局進行期間，每隔一段時間會觸發一次「鬼抓人模式」；目前間隔 90 秒（自上一次事件結束到下一次預告開始）。
- 每次事件由系統選出一支隊伍擔任鬼隊；該隊仍在迷宮中的成員成為鬼，其他隊伍仍在迷宮中的玩家成為逃亡方。
- 已經抵達塔頂的玩家豁免，不會被轉換成鬼，也不需要回到迷宮參與追逐。
- 鬼隊選擇必須公平輪替，不能只依無限制的純隨機選擇，導致同一隊反覆當鬼或某隊始終沒有當鬼。在固定兩隊的規則下，最直接的作法是兩隊交替；第一次由誰當鬼待確認。
- 公平排程實作（2026-09-26）：完全不隨機。追蹤各隊擔任鬼隊的次數，每次選當鬼次數最少且仍有成員在迷宮中的隊；並列時排除上一次當鬼的隊，仍並列則取隊伍代號排序最前者，因此第一次一律由 A 隊當鬼，兩隊嚴格輪流。預告期間若鬼隊全員登塔或只剩一隊在迷宮，本次事件取消並等待下一個間隔。
- 鬼抓人事件只維持一小段時間，目前設定 20 秒，實際持續時間必須可設定。
- 事件的觸發間隔也必須可設定。
- 每次事件必須提前公布下一支鬼隊並顯示倒數計時，例如提前 60 秒顯示「一分鐘後 A 隊成為鬼隊」。
- 預告時間必須可設定；介面應持續清楚顯示即將成為鬼的隊伍與剩餘秒數。
- 在事件期間，鬼隊的目標是接觸並抓到其他隊伍的玩家；其他隊伍則必須躲避鬼隊。接觸的判定：同一層且兩人連續位置距離小於 `ghostEvent.catchRadiusTiles`（初始 0.6 格）。
- 鬼隊在事件期間不能拾取鑰匙或道具。
- 電燈開關屬於場景機關而非背包物品；鬼玩家在事件期間仍可操作尚未使用的電燈開關。
- 鬼隊成員在事件開始前已持有的鑰匙與道具會保留，但在事件期間全部暫時鎖定。
- 鎖定期間不能使用、丟棄或交付既有道具與鑰匙，也不能以既有鑰匙解鎖或登上塔頂。
- 鬼抓人事件結束後，原本持有的鑰匙與道具解除鎖定並恢復正常使用。
- 逃亡玩家被抓到時：
  - 背包內持有的所有道具直接消失。
  - 背包中的量子傳送端點也會直接消失，不享有鑰匙的保留待遇。
  - 身上已取得的鑰匙不會消失、不會掉落，也不會回到原本位置。
  - 成功抓人的鬼玩家取得可設定的額外分數。
- 被抓玩家留在原地並短暫定身；定身時間必須可設定。
- 定身結束後，玩家取得一小段防重抓保護時間，避免同一名玩家被守在原地連續刷分；保護時間必須可設定。
- 被抓不會改變玩家所在位置，也不會將玩家傳回中心塔或其他重生點。
- 鬼的移動速度略高於一般玩家，初始可以從約 `10～15%` 的加速開始試玩，最終倍率必須可設定。
- 鬼目前不具有額外衝刺技能；除移速外是否需要其他能力，應在基礎追逐完成試玩後再決定。
- 事件結束後是否有短暫保護時間，尚待確認。
- 介面：倒數下方以黃色膠囊顯示「n 秒後 X 隊變成鬼」，事件中改為紅色脈動（自己是鬼時為紫色）；名單中鬼的暱稱後加 👻，被定身者加「定身」標記。鬼的角色外觀不由程式著色，之後由設計者以專屬 glb 模型替換。

## 14. 初步美術方向

- 迷宮環境以簡單方塊與純色色塊為主。
- 整體幾何感可以參考《Minecraft》的方塊式空間概念，但目前不表示要採用體素系統或仿製其美術風格。
- 環境只需清楚表達道路、牆面、牆頂、樓梯和中心塔，不需要精緻貼圖或大量細節。
- 鑰匙、道具箱等物件的正式模型由設計者以 glb 製作（obj 先轉 glb），放在 `apps/client/public/models/`，檔名對應 `apps/client/src/render/models.ts` 的清單。啟動時載入，缺檔自動退回程式畫的佔位模型；載入後自動縮放到清單指定大小、底部貼地、xz 置中，設計者不必在意匯出尺度與原點。
- 模型是靜態資源，由網頁伺服器提供並由瀏覽器快取，絕不經由遊戲連線傳輸；連線只傳位置與狀態。
- 未拾取的鑰匙頭頂有一道垂直向上的光柱，不受場景光照影響，全圖黑暗時也看得見，作為尋找鑰匙的線索；高度、粗細與透明度為客戶端可調參數。
- 人物暫用 Kenney Mini Characters（CC0）12 款含骨架動畫的 glb（`apps/client/public/models/characters/`），依玩家 id 決定性分配，所有客戶端一致；站立播 `idle`、移動播 `walk`，腳下以隊色圓盤標示隊伍。輪椅模型不收錄。人物高度縮放為 0.9 格。
- 人物與道具是主要美術投入項目，精緻度應高於環境。
- 具體人物風格、道具種類、材質、燈光及攝影機角度待後續定義。

## 15. AI Agent 目前的工作規則

- 目前處於需求蒐集階段，不得擅自將未確定選項定案。
- 新需求必須整合進既有章節；避免只在文件末尾堆疊零散筆記。
- 發現需求矛盾時，先保留兩種說法並標示待確認，不得靜默刪除任何一方。
- 不要因提到《Minecraft》就自行加入挖掘、建造、破壞方塊、合成或體素世界等玩法。
- 技術棧已於第 17 節定案。不得引入第 17 節以外的遊戲引擎、渲染框架、物理引擎或連線框架。
- 所有遊戲規則、計分、放置物生命週期、鬼隊排程與勝負判定都必須實作在 `packages/sim`；`apps/client` 只負責輸入與渲染，不得持有任何規則判定。
- `packages/sim` 不得依賴 DOM、Three.js、Node 專屬 API 或任何網路套件，必須能在瀏覽器、Node 與測試環境中以相同結果執行。
- 不得將回合時間、背包容量、黑暗視野半徑、陷阱時間或放置物生命週期等平衡數值散落硬編碼在遊戲邏輯中。
- 鬼隊選擇必須實作可測試的公平排程；不得以每次獨立亂數抽選作為最終規則。
- 放置物必須有伺服器或權威遊戲狀態管理的生命週期與清理機制，並防止永久封鎖必要路徑。
- 計分事件必須由權威遊戲邏輯判定，避免客戶端自行回報名次、鑰匙或抓人分數。
- 實作範圍以第 18 節目前所在階段為限，不得跨階段提前實作後續系統。
- 每個可運作的里程碑都應建立 git commit，保留可退回的已知良好狀態。

## 16. 待確認清單

- 正式遊戲名稱是 `Super Maze`、`Maze Tower`，或其他名稱。
- 回合時間依玩家數、地圖尺寸與難度調整的正式計算公式。
- 登頂名次、鑰匙與抓人事件的確切分數表，以及 `2×` 勝隊倍率套用在哪些分數上。
- 完成一層後，是進入同一座塔的下一層，還是前往下一座獨立的塔。
- 中心塔與樓梯的完整操作方式（目前登塔與開關共用情境動作鍵）。
- 橋是否可能被道具阻擋。
- 角色之間是否有碰撞判定（目前穿透），與道具碰撞判定一併決定。
- CPU／AI 的難度、行為和排名資格。
- 斷線玩家可重連的等待期限，以及重連失敗後是否讓 CPU 控制至本局結束。
- 2.5D 攝影機的確切角度、距離與視野範圍。
- 道具生成、拾取、放置和敵我互動的詳細規則。
- 量子傳送機尚未確認的其他邊界行為。
- 隊伍記號的種類、數量上限與存在時間。
- 鬼抓人事件預告時間的正式數值（間隔 90 秒、持續 20 秒、A 隊先當鬼已定）。
- 鬼是否需要移速以外的能力，以及事件結束後是否有保護期。
- 伺服器託管與部署方式。

## 17. 技術架構決策

> 本節於 2026-09-25 定案。第 15 節中關於「不預設技術棧」的限制已由本節取代。

### 17.1 平台與客戶端

- 主要平台為網頁瀏覽器，桌機與手機瀏覽器都是一級目標。
- 客戶端使用 Three.js + TypeScript + Vite，渲染目標為 WebGL2。
- 不使用完整遊戲引擎（Unity、Godot、Babylon.js、PlayCanvas 等）。理由：網頁優先、環境幾何簡單、需要小體積與快速載入。
- 不使用物理引擎。角色移動、碰撞與層間轉換以格子圖與導航圖實作，直接對應第 6 節的拓撲限制。
- 人物與道具模型使用 glTF 格式；核心迴圈驗證好玩之前，一律使用方塊與色塊佔位。
- 目標裝置為手機橫向與桌機鍵盤。手機直立時顯示轉為橫向的提示，不為直立畫面另做版面。
- 觸控操作是一級輸入方式，與鍵盤同時設計；輸入層必須抽象成「玩家意圖」，不得讓玩法程式直接讀取鍵盤或觸控事件。
- 遊戲中介面（HUD）以純 HTML 疊在畫布上，不進 Three.js 場景：上方中央大字倒數與「已登塔 n / 總數」，最後 30 秒轉紅脈動；左上我方、右上敵方名單，每列顯示隊色、暱稱、持鑰匙／已登塔名次／CPU 接管標記與分數；左下三格先進先出道具槽，最左格加亮框表示下一個會用的；右下單一情境動作鍵；倒數下方為兩秒淡出的事件提示。HUD 只讀權威狀態並以前後狀態差異推導提示，不含任何規則。
- 結算畫面在回合結束時覆蓋在場景上：勝隊名稱與隊色、判定原因、依最終分數排序的名單（隊色、暱稱、登塔名次或未登塔、基礎分、勝隊 ×2 標記、最終分），自己那列加亮。線上模式顯示「n 秒後回到大廳」與離開房間；沙盒提供再玩一次與回首頁。
- 開發用的文字狀態列預設隱藏，按 F3 切換。
- 避免使用 WebView 不支援的瀏覽器 API（例如 SharedArrayBuffer、WebGPU 專屬功能），以保留日後以 Capacitor 包裝為 iOS／Android App 的可能性。App 上架本身不在現階段範圍。

### 17.2 權威模擬層

- 所有遊戲規則集中在 `packages/sim`，為純 TypeScript、無外部執行環境依賴的模擬套件。
- 模擬以固定 tick 推進，預設 tick 頻率由調校參數定義。
- 所有隨機行為使用帶種子的偽亂數產生器；相同種子與相同輸入序列必須產生完全相同的狀態，並以自動化測試驗證。
- 同一份模擬程式同時用於：伺服器權威狀態、單機模式、CPU 機器人決策、回放與測試。
- 客戶端只送出輸入意圖並渲染伺服器快照；斷線接管只是將輸入來源切換為 CPU，角色狀態不變。

### 17.3 伺服器與連線

- 伺服器使用 Node.js + TypeScript，每 tick 執行一次模擬並廣播狀態快照與事件。
- 傳輸層使用 WebSocket；不使用 WebRTC。
- 連線框架定案為 Colyseus 0.18（伺服器端 `@colyseus/core` + `@colyseus/ws-transport`，客戶端 `@colyseus/sdk`）。2026-09-25 的階段 0 驗證：八個客戶端同房、20 Hz 快照間隔 p95 抖動小於 1 毫秒、伺服器以 `allowReconnection` 讓斷線玩家原地轉為 CPU 並在重連後交還控制權，手動與 SDK 自動重連兩條路徑都通過。
- Colyseus 只作為房間、配對、重連與訊息傳輸層使用；不使用其 Schema 狀態同步。遊戲狀態一律以 `packages/sim` 的快照透過房間訊息廣播。
- 地圖本身從不在對局中傳送：客戶端自帶地圖檔，伺服器只在 welcome 告知地圖 id 與旋轉方向。
- 狀態同步採「完整狀態一次 + 每 tick 差量」：加入與重連時送一次完整狀態；之後每 tick 必送玩家狀態與 tick，其他區段（鑰匙、道具箱、開關、放置物、傳送點、回合資訊）只在序列化內容與上次廣播不同時附上，客戶端合併進本地副本。2026-09-26 實測（maze-01、6 人）：每 tick 訊息 JSON 等值平均約 1.8 KB（改前 3.4 KB），變動 tick 最大約 3.3 KB；msgpackr 線上大小約為 JSON 的三分之一。客戶端解碼每秒合計約 0.6 ms，對畫面更新率沒有可量測影響。
- 若還要降流量，下一步是玩家狀態降至 10 Hz 由既有插補補足，最後才考慮欄位量化。在遠端實測出現問題之前不做。
- 斷線重連等待時間為調校參數 `connection.reconnectWindowSec`，初始值 30 秒；超時後該玩家維持 CPU 控制至本局結束。
- `@colyseus/sdk` 的自動重連預設要求房間已連線至少 5 秒（`reconnection.minUptime`），加入後不滿 5 秒即斷線的客戶端不會自動重連，但仍可用保存的 token 手動重連（客戶端在重新整理頁面時走這條路）。是否調整此門檻待試玩決定。
- 客戶端第一版只做快照插補（渲染落後一個快照間隔）；本機輸入預測與回滾留待後續階段視需要加入。

### 17.4 儲存庫結構

- 使用 npm workspaces 的 monorepo。
- `packages/sim`：規則、狀態、調校參數結構與預設值、地圖資料型別與驗證邏輯。
- `packages/protocol`：客戶端與伺服器之間的訊息型別與編碼。
- `apps/client`：Three.js 渲染、輸入、HUD、除錯覆蓋層。客戶端專屬的手感參數放在 `apps/client/src/tuning.ts`。
- `apps/server`：房間、連線、權威模擬迴圈。
- `tools/map-validator`：對 `content/maps/` 內的地圖資料執行第 6 節要求的預先驗證。
- `tools/net-spike`：以腳本化客戶端對伺服器做負載與斷線重連的自動化檢查，作為連線層的回歸測試。
- `content/maps/`：人工製作的固定地圖資料。
- 第 12 節的所有平衡參數集中在 `packages/sim/src/tuning/`，並附帶單位、預設值與合理範圍。
- 一個檔案一個系統，單檔超過約 200 行就應拆分。

### 17.5 測試

- `packages/sim` 使用 Vitest，必須包含決定性測試（同種子同輸入產生同狀態）與各規則的單元測試。
- 每個玩法系統先在 `packages/sim` 完成規則與測試，再進行渲染與介面。

## 18. 開發階段

> 2026-09-25 調整順序：開發與測試期先以「完全單機、無 CPU」的沙盒驗證地圖、美術與道具場地機制，確認都沒問題後才做多人專屬系統；CPU 對手留到最後。連線傳輸層已在階段 0 驗證完成，之後只做大廳與最佳化。

| 階段 | 目標 | 主要產出 |
| --- | --- | --- |
| 0 | 需求收斂與技術驗證 | 已完成（2026-09-25）：人數、隊形與目標裝置定案；Three.js 雙層迷宮渲染與角色格子移動驗證；八客戶端 WebSocket 同步驗證與連線框架定案（Colyseus） |
| 1 | 單機核心迴圈 | 已完成：地圖格式與驗證器、鑰匙生成與拾取、登塔、回合計時與勝負判定 |
| 2 | 單機場地機制 | 全圖燈光與黑暗半徑、道具箱與加權抽取、背包、五種道具與放置物生命週期、封路檢查、量子傳送配對；全部在單機沙盒中驗證 |
| 3 | 地圖與美術 | 多張地圖與地圖池、glTF 人物與道具、動畫、音效、HUD、手機橫向操作；以單機沙盒反覆試玩 |
| 4 | 多人專屬系統 | 大廳與分隊、鬼抓人公平輪替、隊伍記號與快捷訊息、塔頂俯瞰與塔頂黑暗半徑、快照最佳化 |
| 5 | CPU 對手 | 單人 1v1 的 CPU、斷線接管後的 CPU 行為、難度分級 |
| 6 | 平衡與上線 | 試玩調參、遙測、部署、PWA；之後視需求以 Capacitor 包裝上架 |

- 目前位於階段 2。
- 單機沙盒是預設的開發環境：`npm run dev:client` 不加 `?online` 即可。沙盒中的規則程式與伺服器完全相同，不允許為沙盒另寫一套邏輯。
- 各階段的時程僅供參考，實際以可玩里程碑為準；每個階段結束時都必須是可實際遊玩的狀態。
